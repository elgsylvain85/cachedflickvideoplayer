name: example
description: Cachedflickvideoplayer demo

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cachedflickvideoplayer:
    path: ../
  # cached_video_player: ^1.0.3
  flutter_widgets: ^0.1.12
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true